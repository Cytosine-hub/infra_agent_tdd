# agent.md（放在每个目标仓库根目录，按实际项目修改）

本文件是本仓库的**开发规范**，所有 AI Agent（Claude Code / Codex 等）和人工开发者都必须遵循。

> 提示：为让各家 agent 自动读取，建议在仓库根目录同时放两个一行的引导文件：
> `CLAUDE.md` 与 `AGENTS.md`，内容均为「开发规范见 agent.md，必须先完整阅读并遵循」。

## 开发流程

- 每个需求对应一个 Issue、一个分支（`feature/req-<门户需求ID>-issue-<Issue号>`）、一个 PR。
- 测试先行：先把 Issue 中的验收测试用例转成自动化测试，再写实现，直到测试全部通过。
- 禁止直接向 main 提交；PR 必须 `Closes #<issue>`。
- 提交信息使用中文，格式：`feat(<模块>): <说明> (#<issue>)`。

## 技术栈与目录结构

<按实际项目填写，例如：>
- 前端：Vue 3 + Element Plus，代码位于 `web/`
- 后端：Spring Boot，代码位于 `server/`
- 每个岗位小组的功能放在独立模块目录：`modules/<team>/`（如 `modules/database/`、`modules/network/`）

## 代码规范

- 组件、接口命名使用小驼峰；文件名使用 kebab-case。
- 新功能必须模块自包含：路由、页面、接口、测试都放在本模块目录内，不修改其他模块的文件。
- 公共组件只新增不修改；确需修改公共代码时在 PR 中单独说明并 @ 相关组长。

## 测试

- 测试框架：<Vitest / JUnit 等>
- 运行方式:`npm test`（前端）、`./mvnw test`(后端)
- 每条 Issue 验收用例必须能对应到至少一个自动化测试，测试名中注明用例编号（如 `TC-01`）。
