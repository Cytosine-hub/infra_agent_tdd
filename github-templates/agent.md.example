# agent.md —— <项目名> 给 Agent 的项目说明书

> 放在**每个目标仓库根目录**，按本仓库实际情况填写。所有 AI Agent（Claude Code / Codex）
> 和人工开发者动手前都必须先完整读一遍。
>
> 为让各家 agent 自动读取，在根目录再放两个一行的引导文件 `CLAUDE.md` 与 `AGENTS.md`，
> 内容都是：`开发规范见 agent.md，必须先完整阅读并严格遵循。`
>
> **两条元规则（决定这份文件有没有用）：**
> 1. **保持精简、高信号**——每一行都应该改变 agent 的行为。写成长篇会被略读，等于没写。
> 2. **保持最新**——agent.md 是代码的一部分：目录结构/约定/命令变了，就在**同一个 PR 里**更新它。

---

## 1. 这是什么（一句话定位 + 形态）

<用 2-3 句让 agent 迅速建立心智模型：项目是做什么的、给谁用、运行形态。例如：>
本仓库是团队运维门户网站。前后端分离，前端 Vue3 打包为静态资源，后端 Spring Boot 提供 API，
按岗位小组（数据库/中间件/主机/网络）划分功能模块。合并到 main 后由 CI/CD 自动部署到生产。

## 2. 代码地图（Codebase Map）——最重要，务必保持最新

<让 agent 不用把整个仓库读一遍就知道东西在哪。列出主要目录 + 每个目录负责什么 + 关键入口文件。>

```
web/                    前端（Vue3 + Element Plus）
  src/modules/<team>/   按小组划分的功能模块（新功能进这里）
  src/shared/           跨模块公共组件/工具（只增不改，见 §6）
  src/router.ts         路由注册入口
server/                 后端（Spring Boot）
  .../modules/<team>/   与前端对应的按小组模块
  .../common/           公共基础设施
scripts/                运维脚本
```

- **新功能通常只动**:`web/src/modules/<team>/` 和 `server/.../modules/<team>/`
- **入口文件**：前端路由 `web/src/router.ts`；后端 `Application.java`

## 3. 架构与必须模仿的核心模式

<这一节是"风格一致"的最大杠杆：不要只用文字描述规则，直接贴 1-3 段本仓库真实代码作为范本，
让 agent 照抄结构。挑最能代表本项目写法的模式。例如：>

**模式：一个功能 = 一个自包含模块。** 新增功能照这个骨架:

```ts
// web/src/modules/database/slow-sql/index.ts —— 模块自注册，不碰别人的文件
export const slowSqlModule = defineModule({
  id: "slow-sql",
  route: "/database/slow-sql",
  title: "慢 SQL Top10",
  // 数据、页面、接口调用都收在本目录内
});
```

- 状态管理用 <Pinia / Redux / …>，按 `<约定>` 组织
- 接口调用统一走 `<web/src/shared/api.ts 的 request 封装>`，不要直接 fetch
- 错误处理统一 `<抛 AppError / 返回 Result>`，不要各写各的

## 4. 如何新增一个功能（照做的配方）

1. 在 `<对应模块目录>` 新建自包含模块（命名见 §5）。
2. 在 `<唯一的注册入口，如 router.ts>` **追加**注册——这是唯一允许改的公共文件，只增不改已有项。
3. 先写测试（把 Issue 验收用例转成自动化测试），再写实现，直到全绿（见 §8）。
4. 跑 §7 的 lint/format/test 全部通过。
5. 按 §9 提交并建 PR。

## 5. 命名与文件布局约定

- 目录/文件名:`<kebab-case>`；类:`<PascalCase>`；函数/变量:`<camelCase>`
- 每个模块目录内固定布局:`<index.ts 入口 / api.ts / *.test.ts / components/>`
- <其它本项目特有约定>

## 6. 风格由工具强制（关键——别靠文字约束风格）

<文字风格规则一定会漂移；提交进仓库的格式化/静态检查工具不会。把它们列出来并要求 agent 每次跑。>

- 代码格式:`<Prettier / gofmt / spotless>` —— 提交前必须 `<npm run format>`，不要手工对齐
- 静态检查:`<ESLint / golangci-lint>` —— 必须 `<npm run lint>` 零告警
- 编辑器:`.editorconfig` 已提交，缩进/换行以它为准
- **约定优先级:工具配置 > 本文件文字描述。** 两者冲突时以工具为准，并在 PR 里指出。

## 7. 命令速查

```bash
<npm install>      # 安装依赖
<npm run dev>      # 本地起服务
<npm test>         # 跑测试
<npm run lint>     # 静态检查
<npm run format>   # 格式化
<npm run build>    # 构建
```

## 8. 测试（必须）

- 框架:`<Vitest / JUnit>`，运行:`<npm test / ./mvnw test>`
- 测试先行：先把 Issue 每条验收用例转成自动化测试，**测试名注明用例编号**（如 `TC-01`）。
- 可测性:把可计算逻辑抽成**具名导出的纯函数**来测，避免只能靠端到端覆盖。
- 提交前全套测试必须通过，**不得破坏已有测试**。

## 9. 分支与提交

- 每个需求 = 一个 Issue = 一个分支（`feature/req-<门户需求ID>-issue-<Issue号>`）= 一个 PR。
- 禁止直接向 main 提交；PR 正文必须 `Closes #<issue>`。
- 提交信息中文，格式：`feat(<模块>): <说明> (#<issue>)`。

## 10. 禁区与反模式（会被评审直接打回）

<把本项目最常见的踩雷、最想禁止的做法明确列出来。越具体越有用。例如：>
- ❌ 修改其它小组的模块文件 / 公共组件（`web/src/shared/`）——确需改动单独 PR 并 @ 相关组长
- ❌ 绕过 `<统一 api 封装>` 直接 fetch；绕过 `<统一错误处理>`
- ❌ 引入未在 §7 声明的新依赖而不说明
- ❌ 提交未跑 format/lint 的代码

## 11. 踩坑与经验（持续沉淀）

<这是"agent 记住项目经验"的真正载体：把每次踩过的坑写在这里，比任何会话记忆都持久、且团队共享。>
- <例：本地时区问题——统一用 UTC 存储，展示层转换>
- <例：某接口有并发限制，批量操作需加节流>
